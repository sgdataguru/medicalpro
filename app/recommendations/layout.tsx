export const metadata = {
  title: 'Prescriptive Recommendations | MedicalPro',
  description: 'AI-powered prescriptive recommendations for hospital operations optimization.',
};

export default function RecommendationsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
