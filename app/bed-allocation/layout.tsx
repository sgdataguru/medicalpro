import type { ReactNode } from 'react';

export const metadata = {
  title: 'Bed Allocation | Farrer Park Hospital',
  description: 'Data-driven bed allocation optimization and demand forecasting',
};

export default function BedAllocationLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
