export const metadata = {
  title: 'Anomaly Detection | MedicalPro',
  description: 'Real-time anomaly detection and monitoring across hospital operations.',
};

export default function AnomaliesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
