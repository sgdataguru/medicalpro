export const metadata = {
  title: 'Staff Allocation Optimizer — Farrer Park Hospital',
  description: 'Optimize nurse-to-patient ratios, predict staffing gaps, and reduce overtime costs.',
};

export default function StaffAllocationLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
